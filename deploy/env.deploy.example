# Config used by ./deploy/deploy.sh ON the VPS (gitignored).
# Copy from env.deploy.example.

PUBLIC_ORIGIN=https://pos.mmds.site
VPS_API_PORT=3010
# Frontend API path (same-origin behind nginx — avoids CORS / stale absolute URLs)
VITE_API_BASE_URL=/api/v1
